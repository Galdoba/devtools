команды
    - open -- открывает таблицу
        //таблица печатается в терминале в модальных режимах
        //SEL - режим выбора ячейки
        //EDT - режим редактирования ячейки

ключи для edit
    -i path.csv         --  путь к файлу
    -read_only (-ro)    --  режим только для чтения